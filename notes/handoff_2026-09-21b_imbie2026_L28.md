# Handoff — IMBIE 2026 adopted as the AIS target; L28 refit landed and fitted the LEVEL not the SHAPE; DAIS structure question reopened (09-21 10:05 → 18:45)

**Start here.** Continues `handoff_2026-09-21_lws_observed_l27_champion.md` (its §3 traps stand). CHANGELOG **09-21e–h** are the
primary record (commits `a21d3d4` … `30c084d` on `ladrillo-dev`, all pushed). Memory written today: `ais_amp_leverage_is_a_threshold`,
`imbie2026_ais_target_low`, `dais_structure_not_warranted_by_imbie2026` (REVISED IN PLACE at 09-21h — its first verdict is
superseded); `INDEX_slr` split → `INDEX_slr_post` (morning); `INDEX_ais` now 16.2 KB (over soft).

**STATE AT HANDOFF (2026-09-21 ~18:45):**
- **Champion and the paper's posterior are STILL L27.** The draft is **r9** (`deliverables/GMD.Ladrillo.v1_review-2026-09-21c_L27.docx`):
  Marcus's three r8 comments answered with L27 re-runs (amp leverage 57/23 cm per σ — a THRESHOLD effect, +1σ tips 30 % of
  SSP2-4.5 draws; runtime 1.24×; "up to 31 cm" confirmed, plural corrected). Comment #12 (code availability) still open.
- **The calibration target changed (Marcus's ruling):** `outputs/recalib_targets_ext.csv` AIS = IMBIE 2026 1979–2023 (md5
  eb768cd9…); old file quarantined; identity gate re-frozen on it. ⚠ Every result through L27 / r9 rests on the OLD target —
  `bench_ladrillo`'s "L27*" rows are scored on it; never compare L28's AIS RMSE to them as like-for-like.
- **L28 = L27's objective on the new target** (`run_L28.sh`, `run_L28_stage2.sh`; subsample, postpred, ssp components, joint bands,
  no-tap, benchmark, prior/posterior table, refit precision, 7 vv + 6 SSP-MAGICC paper arms, figures — all in git). **NOT promoted.**
  The 14 vv MAGICC-climate arms were NOT run (FIG 11 / regrowth attribution on L28 would need them, ~1 h).
- Nothing is running. Disk: L28 chains (8 GB) on disk, not in git.

## 1. ⭐ NEXT (Marcus's rulings needed, in order)
1. **The ρ-cap refit** (L28 objective with `rho_ais` ≤ 0.90; `calibrate_mcmc_ext.jl:1490` hard-codes 0.99 — add a `--rho-max`
   flag, mutation-test the identity gate stays green with the default). ~4 h Mac. It decides whether DAIS's physics CAN carry the
   reconciled record's shape or whether a structural steepening is needed (scoping note §4).
2. **Which posterior the paper ships.** L27 (old target, consistent draft) vs L28 (new target, ρ on the bound, AIS projections
   DOWN: SSP2-4.5 2100 12.6 → 8.1 cm) vs a post-structure refit. Do not swap the draft to L28 as is.
3. If a steepening is built: the smallest form is a free exponent on DAIS's ocean-temperature ratio (nests DAIS at 2); the other
   is a fast-dynamics onset within the observed range (large projection consequences — a scientific claim, Marcus's).
4. The 09-16 list: venue (GMD), package extraction for Tony's team, `facts` remote, FACTS/MAGICC scope, pulse analysis.

## 2. WHAT LANDED TODAY (receipts by CHANGELOG entry)
- **09-21e** r9: amp leverage re-run (`julia/diag_ais_amp_leverage.jl`; regression × sd = the draft's definition; perturbation shows the
  bimodality), runtime ×2 (script now writes `_<tag>.csv`), the joint-arm AIS table for the Conclusions.
- **09-21f** IMBIE 2026 downloaded (`data/observations/raw/imbie2026/`, OGL v3, README); `python/diag_imbie2026_vs_targets.py`
  (both bars): old AIS target 22 % low on the modern rate (−1.3σ), L27 −3.9σ; GIS fine; A5 anchor is ABSOLUTE SMB (IMBIE has
  anomalies only — cannot replace it); Mouginot-share test has no power.
- **09-21g** target rebuilt (`prep_recalib_targets_ext.py`, `--ais-frederikse` reproduces the old file byte-for-byte; join window
  1979–1988 flagged as a choice); quarantine; gate re-frozen (logpost(θ₀) 219.64 → 148.36, target proven the only change);
  L28 launched; first structural scoping (`notes/scoping_2026-09-21_dais_structure_vs_imbie2026.md` §1–3).
- **09-21h** L28 result: cumulative fitted (−0.8σ), 2011–17 rate 0.039 vs 0.056 (−2.1σ); physics unmoved, `rho_ais` 0.89 → 0.97
  on the bound, `sd_ais` −4 sd; 1900–78 loss −43 Gt/yr vs target −20; projections DOWN. The reconciled record's loss per unit GMST
  doubles across 0.45 → 1.0 K; DAIS's discharge is linear to 1 % ⇒ verdict revised: a reachable STEEPENING is indicated (§4).

## 3. ⚠ NON-OBVIOUS STATE / TRAPS (adds to the morning handoff §3)
- **Two targets are now in play.** `outputs/recalib_targets_ext.csv` = IMBIE build; `outputs/quarantine/20260921_ais_target_frederikse/`
  = the old one. `diag_imbie2026_vs_targets.py --tag=L27` would now score L27 against the NEW target and overwrite the morning's
  L27 outputs (which were against the old) — don't re-run it for L27.
- **The identity gate's reference is the IMBIE-target objective**; the two older references (`_lws_seeded_20260916`,
  `_ais_frederikse_20260921`) reproduce with their own target/LWS swapped in. Run the gate after every calibrator edit.
- **Figure guards**: a new tag must be declared in BOTH `ladrillo_figs.TAG_DESC` and `plot_ladrillo_memo_figures.TAG_DESC`
  (11 figure steps failed on L28 until declared). Keep the guard.
- **Frozen-copy launches**: `run_L28*.sh` were run from scratchpad copies with `cd` fixed to the repo; the first launch attempt cd'd
  to the scratchpad and (correctly) died on the md5 check — check `ps` for exactly four chains after any launch.
- `pgrep -f "calibrate_mcmc_ext.jl 2000000"` does NOT match the running chains (juliaup's long binary path precedes the script);
  match on `julia.*calibrate_mcmc_ext` or wait on the runner PID.
